# Auto-generated files — collapse diffs and exclude from language stats
web/package-lock.json linguist-generated=true

# Lockfiles are generated artifacts, never hand-merged. On an upstream merge
# conflict, take our side then regenerate (`uv lock` / npm install) so the
# resolved tree reflects the merged pyproject.toml/package.json. The custom
# "ours-then-regen" driver below (configured per-clone via
# scripts/setup-merge-drivers.sh) handles uv.lock automatically; if the driver
# isn't installed git falls back to a normal conflict and you regenerate by hand.
uv.lock              merge=uvlock-ours linguist-generated=true
web/package-lock.json merge=ours
