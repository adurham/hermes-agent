name: web-tavily
version: 1.0.0
description: "Tavily web search + content extraction (keyed, opt-in). Requires TAVILY_API_KEY — https://app.tavily.com/home."
author: NousResearch
kind: backend
provides_web_providers:
  - tavily
